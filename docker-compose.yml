services:
  adapter:
    build: .
    ports:
      - "6789:6789"
    volumes:
      - ./codex-responses-adapter.toml:/app/config.toml:ro
    command: ["--config", "/app/config.toml"]
    restart: unless-stopped
